# Ultra-minimal setup for CodeSandbox (under 50MB total)
Flask==3.0.0
requests==2.31.0
pytz==2023.3

# Production server (lightweight)
gunicorn==21.2.0

# Optional lightweight alternatives (uncomment if needed)
# numpy==1.24.3
# flask-caching==2.1.0